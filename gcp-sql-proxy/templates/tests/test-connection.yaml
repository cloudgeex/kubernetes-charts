apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "gcp-sql-proxy.fullname" . }}-test-connection"
  labels:
    app.kubernetes.io/name: {{ include "gcp-sql-proxy.name" . }}
    helm.sh/chart: {{ include "gcp-sql-proxy.chart" . }}
    app.kubernetes.io/instance: {{ .Release.Name }}
    app.kubernetes.io/managed-by: {{ .Release.Service }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args:  ['{{ include "gcp-sql-proxy.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
